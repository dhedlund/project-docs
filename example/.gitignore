# MkDocs build output
site/

# Python tooling (if a local venv is used to build the site)
.venv/
__pycache__/

# macOS
.DS_Store

# Node / TypeSpec contract projects
node_modules/
_out/
package-lock.json

# npm cache (created when running npm inside a mounted project)
.npm/
