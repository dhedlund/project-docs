<!-- Glossary tooltips. Each `*[term]: definition` line gives every occurrence of
     `term` in prose a dotted underline + a hover tooltip (Material's
     `content.tooltips`). This file is auto-appended to every page via
     pymdownx.snippets `auto_append`, so define a term once here and it's explained
     everywhere. Keep it in sync with docs/glossary.md (the full catalog).

     Matching is case-sensitive and per-token, e.g.:
       *[SLA]: service-level agreement
-->
