# MkDocs build output
site/

# Python tooling (if a local venv is used to build the site)
.venv/
__pycache__/

# macOS
.DS_Store

# Node / TypeSpec contract projects
node_modules/
_out/

# npm cache (created when running npm inside a mounted project)
.npm/

# Plugin caches (d2 render cache, etc.)
.cache/

# Local overrides (ENGINE / BIND / PORT / BRANCH) — see .env.example
.env
