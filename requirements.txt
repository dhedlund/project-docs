# Docs site dependencies (installed into the toolkit image; also usable directly
# with `pip install -r requirements.txt`). Pinned for reproducibility — bump
# deliberately; `make versions` prints what's installed.
#
# mkdocs-material bundles mkdocs, the search/tags plugins, and the pymdownx
# extensions used in mkdocs.yml.
mkdocs-material==9.7.6

# Diagram-as-code: D2 (renders ```d2 blocks via the d2 binary in the toolkit image).
mkdocs-d2-plugin==1.7.0

# Image / diagram zoom (lightbox).
mkdocs-glightbox==0.5.2

# Render OpenAPI specs in-site (bundles Swagger UI assets statically — offline-capable).
# Present in the image; enable per-project in mkdocs.yml only when there's an API to render.
mkdocs-swagger-ui-tag==0.8.0
